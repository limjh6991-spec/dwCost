# 매출원가(제품) 화면 — RMA 개명 + 양품(반품입고) 신설 (2026-09-15)

대상 화면: **제조·제품 원가 > 매출원가(제품)** (C0009007 > TAB090007), 연/월 두 모드.
데이터 흐름: `DOI_STOCK` → `UP_DOI_STOCK_COST` → `DOI_STCO` → `DOI_ProductCOGS`(매퍼 `C0009007_Tab090007`) → 그리드(프론트가 정적 컬럼 정의 사용).

## 요청
1. 입고상세 / 출고상세의 **RMA(반품입고) → RMA(품번변경)** 로 명칭 변경.
2. 출고상세에 **양품(반품입고)** 컬럼 신설 (위치: 양품(출고)와 타계정 사이).

## 배경 진단 (읽기전용)
- 기존 RMA 버킷 = `DOI_STOCK.RMA_반품입고`/`RMA_반품출고` → 202608 각 2,722 (= 기타입고 2,722, in=out) → **품번변경** 성격. (202601~202607 전부 0, 8월 최초 발생)
- "양품 반품입고" 전용 버킷은 DOI_STCO/파이프라인에 **없었음**. 원천 `DOI_STOCK.OUT_RETURN`(출고반품) 컬럼이 존재하나 **UP_DOI_STOCK_COST 미사용 + 202601~202608 전월 값 0**.
- 결정(사용자): **전체 파이프라인 배선** + **표시전용(매출원가/OUT 총액 불변)**.

## 변경 내역

### 프론트 (커밋 → dev jar 재빌드로 반영)
- `js/TAB090007.js`, `js/TAB090007_2.js` (연/월 그리드 정의):
  - `grpRmaIn`, `grpOutRma` 헤더 `RMA(반품입고)` → `RMA(품번변경)`.
  - `grpOutGood` 헤더 `양품` → `양품(출고)` (신규 컬럼과 구분 명확화).
  - 신규 그룹 `grpOutGoodRtn` = **양품(반품입고)** 를 grpOutGood과 grpOutEtc 사이에 추가 (groupShowMode 'always'). fields/columns에 `outGoodRtnQty`/`outGoodRtnAmt` 추가.
- `tab/TAB090007.vue`: 집계 `numberCols`, 월모드 `numberColsCandidates`(`OUT_GOOD_RTN_QTY/AMT`↔camel), 소계 초기화에 신규 필드 추가.

### DB (SSMS 실행 — HQ 운영DB)
1. `DOI_STCO_add_goodrtn_col_260915.sql` — DOI_STCO에 `OUT_GOOD_RTN_QTY int NULL`, `OUT_GOOD_RTN_AMT numeric(15,2) NULL` 추가 (COL_LENGTH 가드).
2. `UP_DOI_STOCK_COST_fix260915_goodrtn.sql` (라이브 기반 ALTER):
   - MODEL_STOCK: `SUM(ISNULL(a.OUT_RETURN,0)) AS OUT_GOOD_RTN_QTY`.
   - CTE carry(layer0/MODEL_EOH_AMT) → INSERT/SELECT 컬럼목록에 QTY/AMT 추가.
   - AMT 표시평가 = `ROUND(OUT_GOOD_RTN_QTY * OUT_UNIT_AMT, 0)` (당월출고단가).
   - **불변 가드**: `OUT_AMT` 산식(BOH+IN+…−OUT_RMA−…), `OUTETC_AMT` 산식, `EOH_AMT`에 신규 버킷 **미포함** → 총액/매출원가 무영향.
3. `DOI_ProductCOGS_fix260915_goodrtn.sql` (라이브 기반 ALTER): STCO_AGG `MAX/SUM(OUT_GOOD_RTN_*)` + 최종 SELECT `COALESCE(SA.OUT_GOOD_RTN_*,0)` 노출.

## 배포 순서
1. `DOI_STCO_add_goodrtn_col_260915.sql` (컬럼 선행)
2. `UP_DOI_STOCK_COST_fix260915_merged.sql` — REWORK 당월순증 크레딧(fix260914_rma) + goodrtn 병합 (2026-09-15 확정)
3. `DOI_ProductCOGS_fix260915_goodrtn.sql`
4. 202608 **재결산**: 병합본은 REWORK 순증으로 **매출원가 변동**(7073 −1,324,109 크레딧) → 하위 재실행 필요. 순서: `UP_DOI_STOCK_BOH`(내부 STOCK_COST EXEC) → `UP_DOI_SALE_COST` → `UP_DOI_SCOF`. 마감 상태면 마감해제 후 재실행·재마감. (goodrtn 버킷 자체는 표시전용·총액 무영향)
5. 프론트: 소스 커밋/푸시 → 팀 "rebuild dev jar" 로 그리드 반영.

## 검증
- 3종 SQL 읽기전용 test-compile 통과(컬럼 ADD 선행 후 두 프로시저 ALTER, 전체 롤백).
- `OUT_AMT`/`OUTETC_AMT` 산식 라인에 `OUT_GOOD_RTN` 미포함 정적검증 통과.
- 프론트 `TAB090007.js`/`_2.js` require 로드 OK, 출고상세 순서: 양품(출고) | 양품(반품입고) | 타계정 | RMA(품번변경).
- OUT_RETURN 202608=0 → 신규 컬럼 표시값 0 (향후 원천 입력 시 자동 반영).

## ⚠️ 배포 조정 필요 (2026-09-15 라이브 재확인)
현재 라이브 상태:
- `UP_DOI_COST` ✅ fix260914_rma 반영됨 (ETC_IN_RMA_QTY/AMT 존재)
- `UP_DOI_EXPEN_MATL` ✅ fix260914b 반영됨 (ETC_IN_RMA_AMT, RMA_UNIT_COST 존재)
- `UP_DOI_STOCK_COST` ❌ fix260914_rma의 **REWORK 당월순증 매출원가 크레딧 미배포** (OUTETC_AMT 항이 평문 ROUND, CASE/-BOH_AMT 없음). RMA 3종 세트 중 2/3만 라이브.

goodrtn base(UP_DOI_STOCK_COST)는 현재 라이브와 **내용 완전 동일**(정규화 대조 확인) → goodrtn 단독 배포는 아무것도 되돌리지 않음.

**확정(2026-09-15): 병합본 채택** → `UP_DOI_STOCK_COST_fix260915_merged.sql` 배포. REWORK 당월순증 크레딧(fix260914_rma 미배포분)과 goodrtn을 함께 반영. goodrtn 단독본은 삭제(대체). merged test-compile 통과.
