# 매출원가 (4) 재고금액평가손실 행 신설 (2026-09-17)

대상: HQ 제품별손익계산서(C0009009/`DOI_PL_ByModel`) + 총원가(C0009010/`DOI_TotalCost_Tree`).
원천: `DOI_재고자산평가.차이`(취득원가−NRV, 저가법 평가손실, 하한 0).

## 요구/확정
- '제품매출원가조정' 아래 **(4) 재고금액평가손실** 행 신설.
- 금액 = 품번(모델)별 `SUM(차이)`, **재공품/제품 무관 합산**(동일 품번 양쪽이면 합산).
- **총 매출원가 + 양산/개발 합계 가산**, **매출총이익·영업이익 차감**(담당자 확정, 두 리포트 통일).

## 금액 (202608/HQ/ACTUAL 실측)
| 구분 | 금액 | 모델(품번→모델키) |
|---|--:|---|
| 양산 | 22,720,007 | 7073(7073P) + 810P(810PP) |
| 개발 | 18,054,777 | 815Y(815YD)+815Z(815ZD)+8169(8169D) |
| 카세트/구매 | 0 | — |
| **합계** | **40,774,784** | |

품번→모델키 매핑 = `#STCO_MODEL` 규칙(품번이 DOI_STCO.MODEL에 있으면 품번, 없고 끝1자 뗀 값이 있으면 그 값). 양산/개발 = `DOI_재고자산평가.구분`, 카세트 = 품번 LIKE 'VN%'.

## 구현 (리포트 SELECT 전용 → ALTER만, 재결산·프론트·JAR 불필요)
### DOI_PL_ByModel (`DOI_PL_ByModel_fix260917_invloss.sql`)
- `#INVEVAL` 신설(#SALEKEY 뒤): 품번→모델키 매핑 후 (구분,model)별 SUM(차이).
- `#MODEL`(MODEL_LIST)에 `#INVEVAL` UNION 편입 — **필수**(개발 815Y/815Z/8169는 매출/폐기 없어 미편입 시 개발합계에서 18M 누락 → 이 3모델은 리포트에 신규 컬럼으로 등장).
- PL_HEAD에 rn12.5 `(4) 재고금액평가손실` 행 추가(LEFT JOIN #INVEVAL).
- #DISPOSE 롤업 뒤: rn5(II.매출원가) += , rn13(III.매출총이익)·rn141(V.영업이익) −= (#INVEVAL, #DISPOSE 선례 동일).
- rn은 12.5로 소수 → #sourceTable rn이 UNION 승격으로 NUMERIC(정렬 정상).

### DOI_TotalCost_Tree (`DOI_TotalCost_Tree_fix260917_invloss.sql`)
- 스칼라 `@EvalLoss/@EvalLossYangsan/@EvalLossDev/@EvalLossCassette` 선언·계산(품번 SUM, 재공+제품 자동합산).
- `#RN`에 tree_id `14.04`/rn `47.5` `(4) 재고금액평가손실` 행.
- 피벗 CTE·최종 ORDER BY의 `TRY_CONVERT(INT,rn)` → `TRY_CONVERT(DECIMAL(9,2),rn)`(47.5 정렬/구분).
- 합계열 CASE(총/양산/개발/카세트): `WHEN rn=47.5 THEN @EvalLoss*`, rn44(V.매출원가)·rn77(VII.총원가) += @EvalLoss*, rn78(VIII.영업이익) −= @EvalLoss*.
- sp_executesql 파라미터 4종 배선.
- ★합계열=스칼라 override 방식((3)제품매출원가조정과 동일) → 개발 3모델이 열이 아니어도 개발합계 정확. (4)행의 개별 모델열은 0(합계는 스칼라).

## TX 시뮬 검증 (읽기전용, 롤백) — 통과
| 항목 | 총합계 Δ | 양산 Δ | 개발 Δ |
|---|--:|--:|--:|
| (4) 재고금액평가손실(신규) | +40,774,784 | +22,720,007 | +18,054,777 |
| 매출원가(II/V) | +40,774,784 | +22,720,007 | +18,054,777 |
| 총원가(VII, TC만) | +40,774,784 | +22,720,007 | +18,054,777 |
| 매출총이익(III)·영업이익(V/VIII) | −40,774,784 | −22,720,007 | −18,054,777 |
두 리포트 (4) 총액 tie-out 일치. 기존 (1)(2)(3)·타 행 불변.

## 배포 (SSMS)
1. 라이브 정의 백업(권장: OBJECT_DEFINITION 저장).
2. `DOI_PL_ByModel_fix260917_invloss.sql` ALTER.
3. `DOI_TotalCost_Tree_fix260917_invloss.sql` ALTER.
4. C0009009/C0009010 화면에서 (4)행이 매출원가 하위 자식으로 렌더·합계 반영 확인(프론트 무변경).
- 재결산/JAR 재빌드 불필요. 범위=HQ 전용(VN은 별개 개체·데이터 없음).

## 유의
- 향후 월: `차이`가 기존 (3)조정 경로(DOI_COST.LOSS·OUT_DISPOSE·기타매출)와 중복되지 않는지 월별 재확인(202608은 LOSS·폐기 0으로 중복 없음).
- 라이브가 repo(fix260916b)보다 최신이라 **라이브 정의 위에서 ALTER** 함(live-proc-first).
