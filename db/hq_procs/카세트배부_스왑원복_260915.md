# 카세트팀 가공비 배부 — 260914 스왑 원복 (2026-09-15)

대상 DB: 도우제조원가시스템 (10.100.40.17,14233) / 202608 HQ ACTUAL
배포 파일: **`UP_DOI_EXPEN_MATL_fix260915e_cassette_unswap.sql`** (라이브 정의 기반, STEP9 RMA 등 전부 보존)

---

## 배경 — 260914 스왑은 오판이었음
- `카세트가공비_담당자방식_UTG스왑_260914`에서 카세트팀 가공비 배부를 **VINA_CST(0.198) → UTG(0.802)** 로 스왑,
  카세트 제품(VN034P) 노무+경비를 23,232,469 → **89,741,011** 로 올렸음.
- 그러나 담당자 제공 **3차 배부 엑셀(`08월 카세트팀 원가 배부(260914) 3차.xlsx`) = 7월 동일 기준**은
  카세트 제품 = **23,232,469** (VINA_CST 0.198).
  - "카세트 원가명세": 카세트 열 노무 12,796,508 / 당기총제조원가 23,232,469 (UTG 88,311,011 별도).
  - Sheet1 "VINA강화카세트배부" 배부받은금액 TOTAL = 23,232,469.
  - 배부기준: ② VINA 강화카세트(CST) → ③ 카세트 양산(VN034P), 실적비중 UTG 0.8020 / VINA 카세트 0.1980.
- 현행 라이브 89,741,011 = 정확히 UTG 총액(88,311,011) + 외주가공비(1,430,000). → 카세트가 UTG(소비제품) 몫을 잘못 수령.
- 스왑 전 스냅샷 `DOI_EXPEN_MATL_bak260914_hq` = 23,232,469 (엑셀과 원 단위 일치).

## 수정 (스왑 6곳 원복)
| 위치 | 현행(스왑) | 원복(=7월/엑셀) |
|---|---|---|
| STEP 5 (소비제품 배부, 3곳) | `ELSE c.VINA_CST END` | `ELSE c.UTG END` |
| STEP 7 (VN034P 배부, 2곳) | `ELSE c.UTG END` | `ELSE c.VINA_CST END` |
| STEP 7 sub_raw 필터 (1곳) | `c.utg != 0` | `c.vina_cst != 0` |

예외 유지: 53020010 외주가공비 → 100% VN034P(1,430,000), dept448 소모품 → 100% 제품. test-compile 통과.

## 원복 결과 (읽기전용 확인)
| model | 현재(스왑) | 원복(=엑셀) |
|---|--:|--:|
| VN034P1 | 24,580,056 | 6,363,362 |
| VN034P2 | 59,238,057 | 15,335,775 |
| VN034P3 | 5,922,898 | 1,533,332 |
| **합계(노무+경비)** | **89,741,011** | **23,232,469** |

- 카세트 −66,508,542 ↔ 소비제품(818V 등) +66,508,542 **재분배**. 요소별·전체 제조원가 총액 불변.
- downstream: 제품재고 BOH/EOH·매출원가가 스왑 전(담당자 일치) 상태로 복원.

## 적용 (SSMS, 202608 HQ 전체 재결산) — 양품 재정의와 동시 배포
```
1) ALTER: UP_DOI_EXPEN_MATL_fix260915e_cassette_unswap.sql   (카세트 원복)
2) ALTER: UP_DOI_STOCK_COST_fix260915d_docsales.sql          (양품 표시분해, 총액불변)
3) ALTER: DOI_ProductCOGS_fix260915d_docsales.sql            (리포트, 저장 OUT_GOOD)
4) 202608 HQ 전체 재결산(경비집계부터):
   UP_DOI_EXPEN_MATL → UP_doi_mat_amt → UP_DOI_MAT_COST → UP_DOI_COST
   → UP_DOI_STOCK_BOH(내부 UP_DOI_STOCK_COST) → UP_DOI_SALE_COST → UP_DOI_SCOF
```
프론트는 기존 빌드에 양품(반품입고) 컬럼 포함 → 재빌드 불필요.

## 적용 후 대사 (읽기전용)
```sql
-- 카세트 노무+경비 = 23,232,469
SELECT SUM(CAST([in] AS BIGINT)) FROM DOI_EXPEN_MATL
 WHERE YYYYMM='202608' AND SITE='HQ' AND sel_code='ACTUAL'
   AND model LIKE 'VN034P[0-9]' AND EXPEN_SEL NOT IN ('MDAX','MIAX');
-- 매출원가 총액(불변 확인) = 3,833,817,584 근방 ± 카세트 재분배 영향(제품별만, 총액 동일)
SELECT SUM(ISNULL(OUT_AMT,0)) FROM DOI_STCO WHERE YYYYMM='202608' AND SITE='HQ' AND SEL_CODE='ACTUAL';
-- 양품 분해 불변식(모델별 OUT_GOOD_AMT+OUT_GOOD_RTN_AMT = OUT_AMT)
```
