# RMA R/W(7073) 재공 재투입 + 매출원가 표시 재분류 (2026-09-15, 담당자 확정)

결산 재실행 후 7073 RMA R/W 금액 이상 → 담당자 확정값으로 정정. 대상: 202608 HQ.

## 확정 목표 (7073)
- **재공(제조원가)**: RMA R/W 재투입 = **6,821,974** (전월 RMA재고 단가 26,441.76 × 258). 타계정입고 상세합 = 6,821,974.
- **매출원가(제품) RMA 구분**: 양품 −8,146,083 / 타계정(OUTETC) 6,821,974 / 공정재투입 6,821,974 / RMA 0 / 출고(순) −1,324,109.
- **P&L 매출원가(SALE_COST)** = OUT_AMT − OUTETC_AMT + OUT_RMA_AMT = −1,324,109 − 6,821,974 + 0 = **−8,146,083** (재투입 제품 전액 크레딧, 담당자 확정).

## 근본 원인
- 배포된 UP_DOI_EXPEN_MATL STEP9가 **"v2"**(2026-09-14): RMA 재투입단가를 ERP 품목별투입조회 투입금액(`r.RW_AMT`=8,146,083)으로 평가. 주석 "전월단가 방식은 ERP 대비 차이나서 되돌림". → 담당자는 전월단가(6,821,974) 원함.
- 이로 인해 재공 8,146,083 + 병합 STOCK_COST의 REWORK순증 크레딧 1,324,109 = **1,324,109 이중계상** 발생.

## 수정 (라이브 기반 ALTER, SSMS 실행)
1. `UP_DOI_EXPEN_MATL_fix260915_rma_prevunit.sql`: STEP9 unit_cost/[in]/ETC_IN_RMA_AMT 를 `ROUND(전월 RMA단가 × 수량)` 로. v2(r.RW_AMT) 되돌림. → 재공 재투입 6,821,974.
2. `UP_DOI_STOCK_COST_fix260915b_rma.sql` (구 `_rma_display.sql` 대체): 두 가지 STOCK_COST 수정 통합 —
   - **(RMA R/W)** 블록C 뒤 후처리 UPDATE: 구분='RMA' & OUT_REWORK_QTY<>0 행에 `OUT_AMT = BOH_AMT − OUT_REWORK_AMT(원가)`, `OUTETC_AMT = BOH_AMT`, `OUT_REWORK_AMT = BOH_AMT`. → 화면/P&L 목표.
   - **(양산 반품, 2026-09-15 추가)** 블록C의 `SET OUTETC_AMT = r.rma_amt` → `SET OUT_AMT = OUT_AMT − r.rma_amt, OUTETC_AMT = 0`. 반품크레딧을 타계정(OUTETC)에서 총출고(OUT_AMT)로 접음. → **타계정=0(상세합 일치)**, 양품(OUT−OUTETC)=−RMA_AMT 마이너스 유지, SALE_COST(OUT−OUTETC+OUT_RMA) 순액 불변. 양품(반품입고) 컬럼은 산식 미확정으로 0 유지. 모델당 1행(yang_rn.rn=1). 대상 8모델(7073·7110·8085·810P·8122·8136·8140·902K). dry-run 전건 검증(타계정→0, 양품·SALE_COST 동일).

## 배포 순서
1. `UP_DOI_EXPEN_MATL_fix260915_rma_prevunit.sql`
2. `UP_DOI_STOCK_COST_fix260915b_rma.sql` (RMA R/W 표시 + 양산 반품 타계정→0 통합)
3. 202608 **전체 재결산** (재공·매출원가·P&L 모두 변동): UP_DOI_EXPEN_MATL → UP_doi_mat_amt → UP_DOI_MAT_COST → UP_DOI_COST → UP_DOI_STOCK_BOH(→STOCK_COST) → UP_DOI_SALE_COST → UP_DOI_SCOF. 마감 상태면 마감해제 후 재실행.

## 검증 (재실행 후 확인)
- 재공 7073: ETC_IN_RMA_AMT = 6,821,974, 타계정입고 상세합 = 6,821,974.
- 매출원가 7073 RMA행: 양품 −8,146,083, 타계정 6,821,974, 공정재투입 6,821,974, RMA 0, 출고 −1,324,109.
- 둘 다 라이브 기반 test-compile 통과. 실데이터 dry-run: 단일행(RMA1) BOH 6,821,974·REWORK 8,146,083 → 목표 산출 확인.

## ⚠️ 주의
- P&L 매출원가(SALE_COST)가 RMA 7073 기준 −1,324,109 → −8,146,083 (−6,821,974 스윙). 담당자 확정(전액 크레딧).
- 다른 R/W 모델이 생기면 동일 로직(구분=RMA & REWORK) 자동 적용됨.
