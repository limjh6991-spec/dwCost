# 원가 프로시저 실행 결과 (2025-11-14)

## 📋 실행 개요
- **대상 월**: 202510
- **사업장**: HQ (본사)
- **실행일시**: 2025-11-14 22:28 ~ 22:35
- **실행자**: roarm_m3

---

## ✅ 실행 결과

### 1. 재료비 배부 (UP_DOI_MAT_COST)
**상태**: ✅ 성공

**실행 내역**:
- 삭제: 334건
- 입력: 334건
- 배부 모델 수: 11개

**데이터 무결성 검증**:
```
소스(DOI_MAT_AMT):        48건, 388,111,909원
타겟(DOI_MAT_COST):       48건, 388,111,909원
차이:                             0원 ✅
```

**세부 검증**:
- BOM 미등록 자재: 11개
- 미배부 자재: 0개
- 무결성 검증: ✅ 통과

---

### 2. 재공평가 (UP_DOI_COST_MAT)
**상태**: ✅ 성공

**실행 내역**:
- 삭제: 0건 (최초 실행)
- 입력: 21건

**데이터 무결성 검증**:
```
소스(DOI_MAT_COST):     388,111,909원
타겟(DOI_FAB_COST):     388,111,909원
차이:                            0원 ✅
```

**수정 사항**:
- ❌ 문제: `mat_class`로 GROUP BY 시 집계 오류
- ✅ 해결: CASE 표현식을 GROUP BY에 직접 사용
  ```sql
  group by yyyymm, sel_code, site, 도우모델,
    case when mat_class='원자재' then '직접재료비' else '간접재료비' end,
    case when mat_class='원자재' then '직접재료비' else '간접재료비' end,
    case when mat_class='원자재' then 'MDAX' else 'MIAX' end
  ```

**입력 데이터 구조**:
- MDAX (직접재료비): 원자재
- MIAX (간접재료비): 부자재
- expen_sel명: 재료비

---

### 3. 제품수불 금액 집계 (UP_DOI_STOCK_COST)
**상태**: ✅ 성공

**실행 내역**:
- 삭제: 1,367건
- 입력: 1,367건

**데이터 체크**:
- DOI_FAB_COST: 662건 정상
- DOI_STOCK_BOH: 6,387건 정상
- DOI_STOCK: 92건 정상

---

## 📊 최종 데이터 현황 (202510월 본사)

| 테이블 | 건수 | 금액 | 설명 |
|--------|------|------|------|
| DOI_MAT_AMT | 48건 | 388,111,909원 | 재료비 원천 데이터 |
| DOI_MAT_COST | 334건 | 388,111,909원 | 재료비 배부 (모델별) |
| DOI_FAB_COST | 662건 | - | 가공비 배부 (재료비 21건 포함) |
| DOI_STOCK_COST | 1,367건 | - | 제품수불 금액 |

---

## 🔧 주요 수정 사항

### UP_DOI_COST_MAT 프로시저
**수정 전**:
```sql
DELETE FROM DOI_FAB_COST
WHERE yyyymm= @YYYYMM
  and site  = @SITE
  and acct_name='재료비';  -- ❌ 컬럼 없음

INSERT INTO DOI_FAB_COST (... ACCT_NAME ...)
 select ... '재료비' as acct_name ...
group by yyyymm, sel_code, site, 도우모델, mat_class;  -- ❌ 집계 오류
```

**수정 후**:
```sql
DELETE FROM DOI_FAB_COST
WHERE yyyymm= @YYYYMM
  and site  = @SITE
  and sel_code = @SELCODE
  and EXPEN_SEL IN ('MDAX', 'MIAX');  -- ✅ 올바른 조건

INSERT INTO DOI_FAB_COST (... expen_sel명 ...)
 select ... '재료비' as expen_sel명 ...
group by yyyymm, sel_code, site, 도우모델,
  case when mat_class='원자재' then '직접재료비' else '간접재료비' end,
  case when mat_class='원자재' then '직접재료비' else '간접재료비' end,
  case when mat_class='원자재' then 'MDAX' else 'MIAX' end;  -- ✅ 올바른 GROUP BY
```

---

## 🎯 결론

### ✅ 성공 사항
1. ✅ 재료비 배부 (UP_DOI_MAT_COST) 정상 실행
2. ✅ 재공평가 (UP_DOI_COST_MAT) 수정 및 정상 실행
3. ✅ 제품수불 금액 집계 (UP_DOI_STOCK_COST) 정상 실행
4. ✅ 모든 데이터 무결성 검증 통과
5. ✅ 금액 일치 확인 (차이 0원)

### 📝 참고 사항
- UP_DOI_COST_MAT의 GROUP BY 절 수정으로 집계 오류 해결
- DELETE 조건을 `EXPEN_SEL IN ('MDAX', 'MIAX')`로 변경
- 컬럼명을 `acct_name`에서 `expen_sel명`로 수정
- 모든 프로시저가 202510월 데이터로 정상 실행 확인

---

## 🌐 서버 실행 상태

### 백엔드 (Spring Boot)
- ✅ 실행 중
- 포트: 9090
- URL: http://localhost:9090
- 프로파일: local

### 프론트엔드 (Vue.js)
- ✅ 실행 중
- 포트: 4300
- URL: http://localhost:4300
- 모드: development (local)

---

**작성일**: 2025-11-14 22:35
**작성자**: GitHub Copilot + roarm_m3
